# 24H Life System

個人用 24 小時人生系統 · Gaming HUD 風格

六階段：前夜清障 → 早晨開局 → 深度執行 → 下午推進 → 晚間複盤，附 30 日連線熱圖。

單檔 HTML，用 `localStorage` 存資料，開瀏覽器直接用。

**Live**: 部署後網址會長這樣 `https://<username>.github.io/24h-life-system/`
